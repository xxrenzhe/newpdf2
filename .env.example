# =============================================================================
# 环境变量配置
# 复制此文件为 .env.local 并根据实际情况修改
# =============================================================================

# =============================================================================
# 必需配置 (生产环境必须设置，否则应用无法启动)
# =============================================================================

# 域名锁定白名单 - 逗号分隔，支持通配符
# 示例: "example.com,*.example.com,app.example.org"
NEXT_PUBLIC_ALLOWED_DOMAINS="pdftools.app,*.pdftools.app"

# NextAuth 配置
NEXTAUTH_SECRET="your-super-secret-key-at-least-32-chars"
NEXTAUTH_URL="https://pdftools.app"

# =============================================================================
# 推荐配置 (缺失会显示警告，部分功能可能不可用)
# =============================================================================

# 应用公开 URL
NEXT_PUBLIC_APP_URL="https://pdftools.app"

# Stripe 支付配置 (如不需要支付功能可不配置)
STRIPE_SECRET_KEY="sk_live_xxx"
STRIPE_WEBHOOK_SECRET="whsec_xxx"
STRIPE_MONTHLY_PRICE_ID="price_xxx"
STRIPE_ANNUAL_PRICE_ID="price_xxx"

# Google OAuth 配置 (如不需要 Google 登录可不配置)
GOOGLE_CLIENT_ID="xxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-xxx"

# =============================================================================
# 可选配置
# =============================================================================

# 管理员邮箱列表 (逗号分隔)
# ADMIN_EMAILS="admin@example.com,admin2@example.com"

# Gotenberg 服务 URL (用于文档转 PDF)
# GOTENBERG_URL="http://localhost:3000"

# 访客每日下载限制
# NEXT_PUBLIC_GUEST_DAILY_DOWNLOAD_LIMIT="5"

# 启用客户端存储加密
# NEXT_PUBLIC_ENABLE_STORAGE_ENCRYPTION="false"

# E2E 测试模式
# NEXT_PUBLIC_E2E="0"
